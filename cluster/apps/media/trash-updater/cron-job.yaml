---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: trash-updater
  namespace: media
spec:
  schedule: "*/2 * * * *"
  concurrencyPolicy: "Forbid"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 5
  jobTemplate:
    metadata:
      annotations:
        botkube.io/disable: "true"
    spec:
      backoffLimit: 3
      ttlSecondsAfterFinished: 300
      template:
        metadata:
          annotations:
            botkube.io/disable: "true"
        spec:
          restartPolicy: Never
          containers:
            - name: trash-updater-sonarr
              image: ghcr.io/sob/trash-updater:f23dee6b6ed7218affe6dc4f41b427be953b1039a9c3831d29ceb6e85e1d4ac0
              command:
                - "/bin/bash"
                - "ls -la /"
                # - "/app/trash"
                # - "--config"
                # - "/app/trash.yaml"
                # - "sonarr"
              volumeMounts:
                - name: trash-updater-config
                  mountPath: /app/trash.yaml
                  subPath: trash.yaml
                  readOnly: true
          volumes:
            - name: trash-updater-config
              projected:
                defaultMode: 0640
                sources:
                  - configMap:
                      name: trash-updater-config
                      items:
                        - key: trash.yaml
                          path: trash.yaml
