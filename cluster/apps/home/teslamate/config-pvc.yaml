---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teslamate-postgresql
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: ceph-block
