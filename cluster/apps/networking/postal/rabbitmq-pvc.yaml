---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postal-rabbitmq-v1
  namespace: networking
  labels:
    kasten.io/backup-volume: "enabled"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: ceph-block
